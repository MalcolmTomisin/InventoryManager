import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingStart: 15,
  },
  input: {
    marginVertical: 10,
  },
});

export default styles;
