import {StyleSheet} from 'react-native';
import utils from '../../utils';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: utils.height * 0.05,
  },
});

export default styles;
